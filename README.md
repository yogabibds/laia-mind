# Laia Mind
Coloque suas fotos reais:
- Foto de perfil: `assets/img/pfp.svg` → substitua por `pfp.jpg` (mesmo nome: `pfp.svg` pode ser substituído por .jpg, atualize o src se quiser).
- Hero: `assets/img/hero.svg`
- Galeria: `assets/img/gallery/yoga-1.svg` ... `yoga-4.svg`

Dica: mantenha nomes iguais para não precisar editar o HTML. Se mudar o nome/extension, atualize o `index.html` nos `src` das imagens.
